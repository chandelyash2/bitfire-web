query GetUsers($input: UsersInput) {
  getUsers(input: $input) {
    user {
      _id
      name
      userName
      phone
      password
      status
      role
      availableCredit
      creditLimit
    }
    error {
      message
      code
    }
  }
}
