query GetUsers($input: UsersInput) {
  getUsers(input: $input) {
    user {
      _id
      name
      userName
      phone
      password
      role
    }
    error {
      message
      code
    }
  }
}