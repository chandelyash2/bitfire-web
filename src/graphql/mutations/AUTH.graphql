mutation AuthAdmin($input: AuthInput) {
  authAdmin(input: $input) {
    admin {
      _id
      parentId
      name
      userName
      phone
      password
      role
      status
      creditLimit
      availableCredit
    }
    token
    error {
      message
      code
    }
  }
}