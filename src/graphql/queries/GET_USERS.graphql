query GetUsers($input: UsersInput) {
  getUsers(input: $input) {
    user {
      _id
      name
      userName
      phone
      password
      status
      role
    }
    error {
      message
      code
    }
  }
}