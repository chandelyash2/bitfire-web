mutation AuthLogin($input: AuthInput) {
  authLogin(input: $input) {
    token
    user {
      _id
      name
      userName
      password
      role
    }
    error {
      message
      code
    }
  }
}