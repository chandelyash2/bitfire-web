mutation ChangePassword($input: ChangePasswordInput!) {
  changePassword(input: $input) {
    user {
      _id
      name
      userName
      phone
      password
      role
      status
      creditLimit
      availableCredit
    }
    error {
      message
      code
    }
  }
}